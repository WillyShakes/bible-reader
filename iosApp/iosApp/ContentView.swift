import SwiftUI
import shared

struct ContentView: View {
    var body: some View {
        // TODO: embed App() Compose Multiplatform root composable via ComposeUIViewController
        Text("Bible Reader")
    }
}
