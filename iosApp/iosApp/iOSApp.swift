import SwiftUI
import shared

@main
struct iOSApp: App {
    init() {
        // TODO (Feature e): KoinApplication startup — startKoin(modules: appModules)
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
