package app.rema.bible

import androidx.compose.ui.graphics.ImageBitmap

actual fun shareText(text: String) {
    // TODO (Feature f — AC-F-5): UIActivityViewController with plain text
}

actual fun shareImageCard(bitmap: ImageBitmap) {
    // TODO (Feature f — AC-F-6): convert ImageBitmap to UIImage, share via UIActivityViewController
}
